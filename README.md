# web-3d-cdn

```
模拟 web cdn 地址
```

```

es6 转 es5 并压缩
1、cd 到指定工程目录执行
2、监听转换 npx babel index.js --watch --out-file index.min.js
3、压缩代码 npx uglify-js index.min.js -o index.min.js

打包转换 npx babel index.js --out-file index.min.js

```
